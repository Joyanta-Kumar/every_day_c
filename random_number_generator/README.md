# Random number generator  

For now, it only deals with integers. Float will come gradually.

## How to run it

First, compile it and then,  

```sh
./randint -b [bottom] -t [top] -c [count]
```
or  
```sh
./randint --bottom [bottom] --top [top] --count [count]
``` 

