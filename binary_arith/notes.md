# Operations

## LOAD
Load what?    binary value as char array  
to where?     REGA - a global register
from where?   memory - pointer with an memory address

## ADD
## STORE
Store what?   an integer converted into binary chararray.
to where?     memory location with a pointer
from where?   a specified register.